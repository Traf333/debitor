= simple_form_for @buyer, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = f.input :balance
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    '
    = link_to t('.cancel', :default => t("helpers.links.cancel")), buyers_path, :class => 'btn'
